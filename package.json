{
  "name": "multi-team-agentic-coding-arc",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bunx tsc --noEmit",
    "prepare": "bun run scripts/install-git-hooks.mjs",
    "hooks:install": "bun run scripts/install-git-hooks.mjs"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
